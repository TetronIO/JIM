# Root entry and base OUs for Region A (dc=regionA,dc=test)
# Loaded automatically by Bitnami into the LDAP_ROOT database.
#
# IMPORTANT: When LDAP_CUSTOM_LDIF_DIR contains files, Bitnami skips
# the default tree creation, so we must create the root entry ourselves.

dn: dc=regionA,dc=test
objectClass: dcObject
objectClass: organization
dc: regionA
o: Region A Test Organisation

dn: ou=People,dc=regionA,dc=test
objectClass: organizationalUnit
ou: People

dn: ou=Groups,dc=regionA,dc=test
objectClass: organizationalUnit
ou: Groups
