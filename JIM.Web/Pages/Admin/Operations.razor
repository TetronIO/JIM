@page "/admin/operations"
@using JIM.Application;
@using JIM.Web.Pages.Admin.Components
@attribute [Authorize(Roles = "Administrator")]
@inject JimApplication Jim

<PageTitle>Operations</PageTitle>
<MudText Typo="Typo.h4">Operations</MudText>
<MudBreadcrumbs Items="_breadcrumbs" Class="ps-0"></MudBreadcrumbs>
<MudText Typo="Typo.subtitle1" Class="mud-text-secondary">
    Manage worker tasks, view execution history, and configure schedules.
</MudText>

<MudTabs Elevation="0" Rounded="true" ApplyEffectsToContainer="false" Outlined="true" PanelClass="pt-5" Class="mt-5">
    <MudTabPanel Text="Queue" Icon="@Icons.Material.Filled.Queue">
        <OperationsQueueTab />
    </MudTabPanel>
    <MudTabPanel Text="History" Icon="@Icons.Material.Filled.History">
        <OperationsHistoryTab />
    </MudTabPanel>
    <MudTabPanel Text="Schedules" Icon="@Icons.Material.Filled.Schedule" Disabled="true">
        <MudAlert Severity="Severity.Info" Class="mt-3">
            Scheduling functionality will be available in a future release.
        </MudAlert>
    </MudTabPanel>
</MudTabs>

@code {
    private readonly List<BreadcrumbItem> _breadcrumbs =
    [
        new("Home", href: "/", icon: Icons.Material.Filled.Home),
        new("Operations", href: null, disabled: true)
    ];
}
