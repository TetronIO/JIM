namespace JIM.Models.Logic
{
    public enum SyncRuleDirection
    {
        Import = 0,
        Export = 1
    }

    public enum SyncRuleStatus
    {
        Disabled = 0,
        Enabled = 1
    }
}
