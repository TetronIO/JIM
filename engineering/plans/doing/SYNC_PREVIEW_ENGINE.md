# Sync Preview Engine - Implementation Plan

- **Status:** Doing (Phases 0 to 3 complete; Phase 4, full-system preview with sampling, next)
- **Created:** 2026-08-16
- **Issue:** [#288](https://github.com/TetronIO/JIM/issues/288)
- **PRD:** [PRD_SYNC_PREVIEW_ENGINE.md](../../prd/doing/PRD_SYNC_PREVIEW_ENGINE.md) (decisions D1 to D5 settled Aug 2026; recorded there)

## Overview

One pure evaluation core, called by two callers: the real synchronisation pipeline (which then applies and persists) and the preview paths (which return a speculative outcome tree and persist nothing). This is PRD decision **D1 = Option C**, and it is delivered **outbound first**: the outbound extraction is the risky half, it is the half #1115 (the destructive-toggle preview adapter) is blocked on, and the Scenario 8 performance baselines exist to gate it.

The decision rests on two facts verified against the code, not on preference:

1. **The inbound half of Option C already exists.** `SyncEngine` (four partials) is a pure decision engine; its own doc comment: "no I/O, no async, no database access; plain objects in, decision records out". `SyncImportTaskProcessor` orchestrates it, accumulates its decisions in memory, and bulk-persists at page boundaries. Nothing inbound needs restructuring; it needs a preview orchestrator.
2. **The outbound half is the braid, and the braid is not the performance work.** `ExportEvaluationServer` (3,280 lines) interleaves evaluation with staging: six staging call sites, thirty-nine repository touchpoints. The export performance programme (`plans/done/EXPORT_PERFORMANCE_OPTIMISATION.md`, PR #334 onward) optimised the *execution* pipeline (per-CSO save round-trips, LDAP pipelining, `MaxParallelism`), and its optimised staging paths already accumulate and bulk-persist (the plural `CreatePendingExportsAsync` sites). The four remaining singular per-object staging sites are the unoptimised older shape. Unbraiding converts them to the batch pattern: it continues the performance programme's direction, and the expectation is neutral-to-positive throughput, proven against the recorded baseline rather than argued.

## Business Value

- Unblocks the remainder of the Configuration Change Preview framework (#827): #1115 needs the evaluation-only outbound path; Wave 3 (G1/G2) needs the whole engine.
- Delivers the #288 capability itself: previewing the full causal chain for an object with a provable zero-side-effect guarantee.
- Leaves the sync engine architecturally better than it found it: outbound evaluation gains the pure-core shape inbound already has, and future caching and parallelism redesigns operate on the orchestration and persistence layers without touching (or threatening) the semantics.

## Technical Architecture

### Target shape

```
                       +---------------------------------------------+
                       |          SyncEngine (pure, partials)        |
                       |  inbound: projection / join / Attribute     |
                       |  Flow (exists today, untouched)             |
                       |  outbound: SyncEngine.ExportEvaluation.*    |
                       |  (new partials, extracted in Phase 1)       |
                       +-----------+---------------------+-----------+
                                   |                     |
                 decisions         |                     |         decisions
                                   v                     v
   +-------------------------------+----+     +----------+----------------------------+
   | Real path (persists)               |     | Preview path (never persists)         |
   | SyncImportTaskProcessor (inbound)  |     | SyncPreviewServer                     |
   | ExportEvaluationServer (outbound:  |     | returns SyncOutcomeNode tree (D4)     |
   |  orchestrate -> accumulate ->      |     | backstop: rolled-back transaction     |
   |  bulk persist -> apply)            |     |  + read-only facade (defence in depth)|
   +------------------------------------+     +---------------------------------------+
```

- **Purity is structural, not disciplinary:** the `SyncEngine` class carries no repository, context, or connector field today, and the new partials keep it that way. A preview cannot write because the code it calls has nothing to write with; the transaction/facade backstop exists for the orchestration around it.
- **Configuration is a parameter (D5):** `SyncEngine`'s existing methods already take their configuration as arguments (`EvaluateProjection(cso, activeSyncRules)`); the extracted outbound methods follow the same style, which is what lets #827 adapters later pass a *proposed* configuration.
- **In-run working set:** evaluation currently reads back what the same run staged (e.g. `EnsureDeletePendingExportAsync`'s per-object lookup) to avoid duplicate decisions. Unbraided, "what have we already decided this run" is an in-memory index the core consults; the database is only asked about *pre-existing* Pending Exports, through the already-batched lightweight lookups.
- **Reads stay prefetched:** `BuildExportEvaluationCacheAsync` / `RefreshExportEvaluationCacheForPageAsync` already page-batch evaluation inputs; the orchestrator keeps them. Mid-evaluation point reads move orchestrator-side: export matching (`AttemptExportMatchingAsync`) resolves candidates before the engine decides, mirroring how the inbound processor feeds `EvaluateJoin`.
- **What is apply, not evaluation:** `CreatePendingProvisioningCsoAsync`, `AddSecondaryExternalIdToCsoAsync`, and all staging remain orchestrator-side. The decide/apply line is drawn per method during extraction and recorded in the code as it is drawn.

### Delivery mechanics

Each phase lands as its own PR (Phase 1 as a short sequence of PRs, one per method family). `main` moves quickly and CodeQL reviews whole PR diffs at open time; small sequential PRs avoid both the long-lived-branch findings wave and conflict debt. Every extraction PR must leave the full suite and the touched integration scenarios green: there is no "broken until the last PR" state.

## Implementation Phases

### Phase 0: Baseline and guardrails ✅

- [x] Capture the Scenario 8 performance baseline on current `main` (default fast-writes mode, recorded in `results/performance/`), so every Phase 1 PR has a fixed comparison point. Record template and host alongside. **Captured 2026-08-18** in the cloud sandbox: Small template, OpenLDAP, scenario execution **329.0s** (`TestDurationMs` 328,980; 67 operation timings in the JSON), full scenario green. One caveat that binds every later gate: `results/` is gitignored and sandbox hosts are ephemeral, so a baseline is only comparable **within the session that captured it**. Each Phase 1 session therefore baselines `main` first and compares its branch in the same sandbox; the figure recorded here is the shape of the cost, not a cross-host constant. Two sandbox potholes hit and recorded on the way: the CA guidance in `test/CLAUDE.md` was wrong twice over (fixed there), and the first passing run still exited 1 because of a #1382 regression in the scenario invoker, fixed as this phase's stack layer so Phase 1's gates can trust the exit code.
- [x] Add the isolation-assertion helper the PRD requires (req. 10): given a database, snapshot Pending Export count, MVO count and attribute versions, CSO count, RPEI count, Activity count; assert unchanged after a preview call. Built now so every later phase can use it from its first test. **Delivered** as `DatabaseIsolationSnapshot` (JIM.TestSupport), watching eight tables (the PRD's five populations plus the attribute-value and change child tables that give them content) with a row count **and a content digest** per table, because an in-place UPDATE leaves the count identical and is exactly the leak a count-only assertion cannot see. Reads through raw Npgsql so the instrument shares nothing with the code it checks; a watched table going missing fails the capture by name rather than silently narrowing the guarantee. Written red-first; six pure comparison tests plus four `RequiresPostgres` tests, including the update-in-place case, all green.

### Phase 1: Outbound extraction (the unbraiding) ✅

Method family by method family, each PR: write characterisation tests over the family's current decisions first, extract the pure decision into a `SyncEngine.ExportEvaluation.*` partial, convert the family's staging to accumulate-and-bulk-persist, prove the characterisation tests still pass, run the affected integration scenarios.

- [x] **1a: In-run working set.** Introduce the staged-decision index; convert `EnsureDeletePendingExportAsync`'s per-object read-back (line ~827) to consult it, retaining the batched database lookups for pre-existing Pending Exports. **First half delivered (first extraction PR):** `ExportEvaluationWorkingSet` exists and the set-based deletion path records every verdict in it. **Completed (second extraction PR):** the working set now records every Delete Pending Export a run stages (recorded after the persist succeeds, so a failed batch write cannot leave it claiming a phantom), `EnsureDeletePendingExportAsync` consults it before paying its per-object read, and `FlushPendingMvoDeletionsAsync` shares one working set between the bulk deletion evaluation and its per-MVO fallback, so the fallback reuses the bulk attempt's staged exports from memory.
- [x] **1b: Attribute delta and create/update decisions.** The `CreateOrUpdatePendingExportAsync` pair and `CreateOrUpdatePendingExportWithNoNetChangeAsync`: delta computation and change-type verdicts into the engine. **Delivered (third extraction PR):** `DecideOutboundStaging` carries the change-type verdict (Object Type conflict reporting, the provisioning gate, the #1003 recall guard, pending-provisioning reuse or irrelevance, update), `MergeAttributeChangesIntoPendingExport` carries the in-memory merge with the #1199 whole-attribute supersede, and the whole delta computation (`ComputeAttributeValueChanges` plus its comparison, merge-key and dictionary helpers) moved into `SyncEngine.ExportStaging.cs`, taking the expression evaluator as a parameter in the engine's established style; `ExportEvaluationServer` shrank by ~970 lines to orchestration plus thin delegating wrappers, so the existing OutboundSync characterisation suites pin the moved computation unchanged through the same entry points. Export matching and its atomic claim stay orchestrator-side (data access), turning a ProvisionNewCso verdict into an Update when a match is claimed. 18 further red-first engine tests. **Gate:** full solution suite green (8,273 tests, zero warnings); Scenario 8 Small ran every step green at **320.2s against the same-session 305.5s `main` baseline (+4.8%, within the 10% tolerance)**. Note: the singular staging sites still persist per object inside the wrappers; converting them to accumulate-and-bulk-persist rides with Phase 2's evaluation-only path, where the accumulate step is introduced anyway.
- [x] **1c: Deprovisioning and deletion decisions.** `HandleOutboundDeprovisioningAsync`, `EvaluateMvoDeletionAsync` / `EvaluateMvoDeletionsAsync`, `EnsureDeletePendingExportAsync`'s verdict. **Deletion half delivered (first extraction PR):** `DecideMvoDeletionExport` is the first `SyncEngine.ExportEvaluation` partial, carrying the #655 verdict (rule matching on the system and object type pair, Delete wins a conflict), the one-Pending-Export-per-CSO collision policy, and the identifier capture; eleven red-first engine tests pin it, and `EvaluateMvoDeletionsAsync`'s two inline verdict blocks now call it with logging, counters and bulk-persist order preserved verbatim. **Gate:** full suite 9,239 green; Scenario 8 Small on the branch ran every step green at **339.9s against a same-session 329.0s baseline (+3.3%, within the 10% tolerance)**. **Out-of-scope half delivered (second extraction PR), completing 1c:** `DecideOutOfScopeDeprovisioning` carries the OutboundDeprovisionAction verdict and the same collision policy (an unrecognised action stays a visible non-action, never defaulted to disconnect), and `ShouldMarkLastConnectorDisconnected` carries the last-connector grace-period predicate; `HandleOutboundDeprovisioningAsync` and the singular `EnsureDeletePendingExportAsync` path swapped onto them with mutations, persistence and logs preserved verbatim. Twelve further red-first engine tests pin the family. **Gate:** full solution suite green (all seven projects, 0 failures); Scenario 8 Small on the branch ran every step green at **308.1s against a fresh same-session 305.5s `main` baseline (+0.9%, within the 10% tolerance)**.
- [x] **1d: Reference recall decisions.** The fast path and fallback (`StageRecallFastPathAsync` / `StageRecallFallbackAsync`): the merge/supersede verdicts into the engine. **Delivered (fourth extraction PR):** `SyncEngine.ReferenceRecall.cs` carries `DecideRecallRemovalChange` (the removal-change verdict: a value-carrying Remove for a multi-valued source, a null-clearing Update for a single-valued one, nothing when a multi-valued removal has no resolvable value), `MergeRecallChangesWithExistingPendingExport` (the #1003 collision policy: Delete wins, Create protected, Update merges with recall winning merge-key collisions and deleted-object references purged), and `PurgeChangesReferencingDeletedObjects` (the fallback's purge); both paths' staging already bulk-persisted and stays orchestrator-side, as do the capture and existence queries. Ten red-first engine tests. **Gate:** full solution suite green (8,283 tests, zero warnings); Scenario 8 Small ran every step green at **305.7s against the same-session 305.5s `main` baseline (+0.1%, within the 10% tolerance)**.
- [x] **1e: Export matching.** Orchestrator resolves candidates (the `AttemptExportMatchingAsync` query stays where the data access is); the engine decides which Object Matching Rules to try and in what order. **Delivered (fifth extraction PR):** `SelectExportMatchingRules` carries the mode switch (Connected System mode reads the Connected System Object Type's shared rules, Advanced mode the Synchronisation Rule's own), the ordering and the missing-navigation guard; the braid's candidate resolution turned out never to materialise a candidate set in memory (the per-rule indexed query returns at most one match), so forcing a candidates-in-memory API would have loaded candidate CSOs per rule for no gain, and the query loop stays orchestrator-side as the item prescribed. Six red-first engine tests. **Gate:** full solution suite green (8,292 tests, zero warnings); Scenario 8 Small ran every step green at **330.1s against the same-session 305.5s `main` baseline (+8.1%, within the 10% tolerance; the widest margin of the five slices, on a sandbox that had run four full gate cycles that day, and the slice adds one list sort per provisioning attempt, so host noise is the credible explanation; watch the Phase 1 gate runs for confirmation)**.
- [x] **Phase gate:** `dotnet build`/`test JIM.sln` green; integration Scenarios 1, 2, 4 and 8 green; Scenario 8 wall-clock within 10% of the Phase 0 baseline. **Closed 2026-08-18:** solution suite green at 8,292 tests with zero warnings; Scenarios 1, 2 and 4 (Small, OpenLDAP) each ran every step green on the 1e branch tip after the fifth slice merged; Scenario 8 green five times across the day's gates, all within tolerance (+0.1% to +8.1% against the same-session 305.5s baseline).

### Phase 2: Evaluation-only outbound path (what #1115 consumes) ✅

- [x] An evaluate-without-apply entry point on `SyncServer` / `JimApplication`: run the Phase 1 core over a population and return decision records; nothing staged, nothing persisted. **Delivered:** `JimApplication.ExportEvaluation.EvaluateOutboundPreviewAsync(mvoIds, repositoryFactory?)` returns an `OutboundPreviewResult` of per-(Metaverse Object, export Synchronisation Rule) decision records: staging verdicts with the attribute changes a real evaluation would stage (no-net-change detected against cached target values), a read-only export matching probe (a real run would claim the match; the preview never claims), and out-of-scope deprovisioning verdicts. The entry point evaluates through a guarded sibling `ExportEvaluationServer`, so every reused read path runs under the write guard. (The `SyncRunMode.PreviewOnly` generalisation folded into this shape: the preview path IS the evaluate-never-persist mode, structural rather than flag-gated, which D1=C's whole point was.)
- [x] Defence-in-depth backstops around the preview orchestration (PRD req. 8): **delivered** as `ReadOnlySyncRepositoryGuard` (all 52 mutating `ISyncRepository` members throw `PreviewWriteAttemptedException`; a reflection sweep pins the classification and a new interface member fails compilation until classified) plus `BeginRollbackOnlyTransactionAsync` (relational transaction rolled back unconditionally on disposal; proven against real PostgreSQL to discard writes made inside it).
- [x] Isolation assertions (Phase 0 helper) wired into the coverage for the new path: `OutboundPreviewIsolationDatabaseTests` (RequiresPostgres) captures a `DatabaseIsolationSnapshot` before and after a preview against a live database and asserts the integrity tables byte-identical (PRD req. 10). A populated-fidelity variant rides with Phase 3's paired fidelity test.
- [x] Notify #1115: its outbound blocker is gone; the adapter issue can start. **Done:** commented on #1115 after #1422 merged, naming the API and the guarantees.

### Phase 3: Preview server and outcome tree ✅

- [x] `SyncPreviewResult` (`JIM.Models/Transactional/`), composing `ExportEvaluationPreviewResult`; the speculative tree as unpersisted `SyncOutcomeNode` DTOs (D4), with one mapping shared by the RPEI display path so preview and reality render through the same component. **Delivered:** seven new model files (`SyncOutcomeNode` with the shared recursive `FromSyncOutcome` mapping, `SyncPreviewResult`, `SyncPreviewInboundSummary`, `SyncPreviewAttributeFlowChange`, `SyncPreviewMessage` + `SyncPreviewMessageCode`, `SyncPreviewSyncRuleReference`), red-first with System.Text.Json round-trip coverage for the serialisability requirement (req. 3).
- [x] `SyncPreviewServer` with `PreviewSyncForCsoAsync` and `PreviewSyncForMvoAsync`: orchestrate the existing pure inbound `SyncEngine` plus the Phase 2 outbound path; blocking `Errors` distinct from `Warnings` programmatically. **Delivered:** `JimApplication.SyncPreview`, both methods running under the Phase 2 backstops (guarded sibling `ExportEvaluationServer` over a `ReadOnlySyncRepositoryGuard`, rollback-only transaction, optional `repositoryFactory` scope). The CSO preview mirrors the processor's inbound sequence read-only: per-rule scoping, a join probe that never claims (an ambiguous match returns an `AmbiguousMatch` error, PRD req. 5), projection evaluation, Attribute Flow onto a preview-owned working copy, then the outbound chain over the prospective state via a new internal materialised-MVO overload (`EvaluateOutboundPreviewForMaterialisedMvosAsync`) factored out of the Phase 2 loop, so a projected object with no id previews outbound too. Expected blocks (`ObjectNotFound`, MVA→SVA flow violations, expression failures) return in `Errors`; out-of-scope and no-applicable-rule land in `Warnings`. Eight red-first server tests plus two live-PostgreSQL isolation tests (`SyncPreviewIsolationDatabaseTests`, the populated-topology variant of the Phase 2 fixture, PRD req. 10).
- [x] **Fidelity paired test (release-blocking, PRD req. 9):** preview an object, really sync it, diff the two outcome trees; a mismatch is the preview lying. **Delivered** as `SyncPreviewFidelityTests` (workflow harness): each test previews first, then really syncs the same undisturbed data, maps the recorded tree through the shared `SyncOutcomeNode.FromSyncOutcome`, and diffs shapes node for node (OutcomeType, DetailCount, child count, sibling order). Two pairings: projection with a provisioning export (Projected → Attribute Flow + Provisioned → Pending Export) and a joined object's changed value (Attribute Flow root). The pairing immediately earned its keep: the real builder's stated Provisioned-under-Attribute-Flow nesting never actually happens (its child lookup keys on `ParentSyncOutcomeId`, which is only resolved at bulk-insert flattening), so export outcomes are recorded as the root's children; the preview mirrors recorded behaviour, with the quirk documented at the mirror site.

### Phase 4: Full-system preview with sampling (D2)

- [ ] `PreviewFullSyncAsync`: whole-population count tier plus a bounded per-outcome-category sample of full trees; explicit work budget (object cap and/or time); truncation flagged in the result. Verified at a Scale template within the budget.

### Phase 5: Consumption surface (D3 = engine + API only)

- [ ] The `JimApplication` API is the v1.0 surface; #827's adapters are the consumer. No portal button, REST endpoint, or cmdlet ships in this milestone (D3's record in the PRD explains why: a portal-only surface would violate the surface parity rule, and full parity is more than the milestone needs). The administrator-facing surface is filed as its own parity-complete issue when wanted.
- [ ] Engineering documentation: the zero-side-effect design (what is structural, what is backstop) written up so future contributors do not introduce a persisting path into preview. No `docs/` or changelog entry: this is a foundation, not yet an administrator-facing feature.
- [ ] Update the #827 plan: Phase 1 marked delivered; Wave 3 (G1/G2) becomes fileable.

## Success Criteria

- The PRD's acceptance criteria, all of them, with the fidelity test and isolation assertions as the release gates.
- Scenario 8 within 10% of the Phase 0 baseline after the last Phase 1 PR.
- `ExportEvaluationServer` contains no evaluation semantics at the end of Phase 1: decisions live in `SyncEngine` partials; the server orchestrates, accumulates, persists, applies.
- #1115 unblocked at the end of Phase 2, without waiting for Phases 3 to 5.

## Dependencies

- None inbound: `SyncEngine`, `SyncOutcomeBuilder` (#363), `ExportEvaluationPreviewResult`, and the integration suite all exist.
- #827's framework consumes the result; #1115 consumes Phase 2 specifically.

## Risks & Mitigations

| Risk | Mitigation |
|---|---|
| Performance regression in export evaluation | Phase 0 baseline; 10% gate on every Phase 1 PR; the conversion direction (per-object writes to batch) is the same one PR #334 measured as a win |
| Half-extraction: core methods accepting tracked entity graphs, quietly reintroducing side-effect risk | `SyncEngine` has no repository/context field, and a test asserts the class's constructor dependencies stay empty; extracted methods take plain objects and return records, reviewed per PR |
| Hot-path landmines (tracked-entity fix-ups, raw SQL writers, `AutoDetectChangesEnabled`) documented in `src/CLAUDE.md` | The apply side is deliberately not restructured: staging conversion reuses the existing plural bulk paths; detach/fix-up code moves untouched |
| Behaviour drift during extraction | Characterisation tests written against current decisions before each family moves; integration scenarios per PR; the Phase 3 fidelity test as the end-to-end proof |
| Reconciliation paths left braided, so previews stop where they take over | `SyncEngine.PreExportReconciliation.cs` is already pure; Phase 1c/1d cover the deprovisioning and recall paths; anything found still braided is extracted in-phase, not deferred |
| Long-lived branch accumulating CodeQL findings and merge debt | One PR per method family, each independently green and merged |
