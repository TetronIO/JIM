namespace JIM.Models.Logic
{
    public enum SyncRuleDirection
    {
        NotSet = 0,
        Import = 1,
        Export = 2
    }

    public enum SyncRuleMappingType
    {
        NotSet = 0,
        AttributeFlow = 1,
        ObjectMatching = 2
    }
}
