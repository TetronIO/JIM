# Development Tools
# This compose file is reserved for optional development tools.
# These tools are NOT included in production/release bundles.
#
# Currently empty - Adminer removed in favour of VS Code database extensions.
# This file is kept for future development tools if needed.
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.override.yml -f docker-compose.dev-tools.yml up -d
#
# Or use the alias:
#   jim-stack-dev

services: {}

networks:
  jim-network:
    external: true
    name: jim-network
